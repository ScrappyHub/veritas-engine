# Veritas Engine Specifications (Authoritative)

This folder is the canonical source of truth for Veritas Engine v1:

- Integrity Evidence Bundle format v1
- Reference verifier toolchain spec v1
- Architecture spec v1
- JSON schemas
- Canonical JSON + hashing + signature rules

Implementations must conform to these specs to be considered compatible.
